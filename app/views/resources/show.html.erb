
<html>
<div align="right">  <%= link_to 'Back', resources_path %> | <%= link_to 'Delete', resource_path(@resource), method: :delete, data: { confirm: 'Are you sure?' } %>
</div>

<p id="notice"><%= notice %></p>

<b>Tags :   </b>
<%= raw @resource.tags.map(&:name).map { |t| link_to t, tag_path(t) }.join(', ') %>


<%= image_tag @resource.pic.url %>
<br/>

</html>




